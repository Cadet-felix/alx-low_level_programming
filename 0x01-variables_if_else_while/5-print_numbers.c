#include <stdio.h>

/**
 * main - Entry point.
 * Description: assign a random number to the variable
 * Return: Always 0.
 */
int main(void)
{
	int x;

	for (n = 0; n < 10; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
